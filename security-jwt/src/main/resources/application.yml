server:
  port: 9088
  servlet:
    context-path: /

spring:
  main:
    allow-circular-references: true
  mvc:
    servlet:
      load-on-startup: 0
  application:
    name: scaffold-security-jwt

  profiles:
    active: dev
